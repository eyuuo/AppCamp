<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.eyuuo.day4">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".PActivity"
            android:parentActivityName=".PActivity"
            android:theme="@style/Theme.AppCompat.NoActionBar" />
        <activity android:name=".TabActivity" />
        <activity
            android:name=".WordActivity"
            android:label="@string/title_activity_word"
            android:theme="@style/AppTheme"></activity>
        <activity
            android:name=".Word2Activity"
            android:label="@string/title_activity_word2"
            android:theme="@style/AppTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".RecycleActivity"
            android:label="@string/title_activity_recycle"
            android:theme="@style/AppTheme">

        </activity>

    </application>

</manifest>